# ONASSIS environment variables
# -------------------------------------------------------------------
# Copy this file to `.env` and fill in real values. The `.env` file is
# git-ignored so secrets never get committed.
#
# Values defined here OVERRIDE matching keys in config.yaml at runtime.
# Anything left blank simply falls back to the config.yaml default.

# --- Core ---
ONASSIS_ENV=development            # development | production
LOG_LEVEL=INFO                     # DEBUG | INFO | WARNING | ERROR

# --- Database ---
ONASSIS_DB_PATH=data/onassis.db    # SQLite file location (relative to repo root)

# --- Scheduler ---
ONASSIS_RUN_AT=08:00               # Daily run time, 24h HH:MM (local time)

# --- Brand identity (used by the Content Director when building briefs) ---
ONASSIS_BRAND_NAME=Onassis Lifestyle

# --- LLM provider (REQUIRED) ---
# Content is generated with the Anthropic API. Set your key here; the agents
# read it via this environment variable.
ANTHROPIC_API_KEY=

# Optional model overrides (otherwise taken from config.yaml -> llm):
# ONASSIS_LLM_MODEL=claude-opus-4-8
# ONASSIS_LLM_EFFORT=high            # low | medium | high
# ONASSIS_LLM_MAX_TOKENS=8000

# --- Fulfilment: Gelato (Sprint 31) ---
# Auto-fulfil paid Etsy orders as Gelato production orders.
GELATO_API_KEY=
# A PUBLIC https base Gelato can fetch print files from. The API serves the
# exports dir at /exports, so this is simply https://<your-server>/exports
# (must be publicly reachable — not localhost — Gelato fetches server-side).
GELATO_FILE_BASE_URL=

# --- Financial Protection Engine (Sprint 36) — CORE COMMERCIAL CONTROLS ---
# These six are the ONLY protection settings exposed to the environment; all fee
# models, estimation and business logic live in config.yaml -> protection.
MIN_GROSS_MARGIN_PERCENT=25
MIN_CONTRIBUTION_MARGIN_PERCENT=18
DEFAULT_RISK_RESERVE_PERCENT=8
MAX_RISK_RESERVE_PERCENT=15
MAX_SINGLE_PRICE_CHANGE_PERCENT=15
MIN_CONFIDENCE_SCORE=0.85
