# ONASSIS — Production Readiness Report

_Generated by the read-only audit in `onassis/production_readiness.py` (`GET /production/readiness` · `python main.py --readiness`)._

**Environment:** `development`  ·  **Overall:** **NOT YET PRODUCTION READY**

**Modules audited:** 28 — 19 Production Ready · 4 Partially Ready · 5 Development Only.  **Open blockers:** 11.

**Credentials detected:** AI provider ✅ · Etsy ❌ · Pinterest ❌.

## Module status

| Status | Module | Notes |
|---|---|---|
| 🟢 Production Ready | `onassis/config.py` — Configuration | Merges YAML + environment; secrets read from env/.env. |
| 🟢 Production Ready | `onassis/database.py` — SQLite database | Schema is created idempotently; integrity check available. |
| 🟢 Production Ready | `onassis/logger.py` — Logging | Rotating file + console logging. |
| 🟢 Production Ready | `onassis/scheduler.py` — Scheduler | Daily trigger; deterministic. |
| 🟢 Production Ready | `onassis/llm.py` — AI provider (Anthropic) | Structured JSON generation via the Anthropic API. |
| 🟢 Production Ready | `onassis/agents/content_director.py` — Content Director | Generates daily briefs via the LLM. |
| 🟢 Production Ready | `onassis/agents/content_creator.py` — Content Creator | Generates campaign content via the LLM. |
| 🟢 Production Ready | `onassis/campaign_manager.py` — Campaign Manager | Orchestrates director + creator into campaigns. |
| 🟢 Production Ready | `onassis/brain.py` — ONASSIS Brain | Predicts and remembers; stores knowledge. |
| 🟢 Production Ready | `onassis/compliance.py` — Compliance Director | Reviews proposals/campaigns; veto authority. |
| 🟢 Production Ready | `onassis/ceo.py` — CEO Agent | Risk-adjusted ROI policy; deterministic decisions. |
| 🟢 Production Ready | `onassis/governance.py` — Governance chain | Compliance veto + CEO decision pipeline; deterministic. |
| 🟢 Production Ready | `onassis/proposals.py` — Proposals | Proposal value objects; deterministic. |
| 🟢 Production Ready | `onassis/profit.py` — Profit Engine | Capital allocation + AI-budget accounting; deterministic. |
| 🟢 Production Ready | `onassis/revenue.py` — Revenue Intelligence Engine | Per-order economics over the unified ledger; deterministic. |
| 🟢 Production Ready | `onassis/optimiser.py` — Product Optimiser | Proposes one action per product; deterministic ranking. |
| 🟢 Production Ready | `onassis/experiments.py` — Experiment Engine | A/B experiments + statistical confidence; deterministic. |
| 🔴 Development Only | `onassis/connectors/etsy_client.py` — Etsy API client (read) | Real client implemented; credentials missing. |
| 🔴 Development Only | `onassis/connectors/etsy.py` — Etsy Operations Connector (read) | Real, tested connector; credentials missing. |
| 🟡 Partially Ready | `onassis/analytics.py` — Analytics Collector | Engine works; Etsy source needs credentials, Pinterest source is not implemented. |
| 🔴 Development Only | `onassis/connectors/pinterest.py` — Pinterest connector | Implements the metrics contract but the live fetch is a no-op; returns no data even when credentials are present. |
| 🟡 Partially Ready | `onassis/listing_factory.py` — Listing Factory | Builds upload-ready Etsy listing packages; copy is LLM-generated, pricing deterministic. Mock-up images are placeholder PNGs. |
| 🟡 Partially Ready | `onassis/publishing.py` — Autonomous Publisher | Publishes compliance-approved packages to Etsy as drafts; idempotent, retried, logged. Live mode is intentionally disabled. |
| 🟢 Production Ready | `onassis/daily_cycle.py` — Daily Cycle | Single execution entry point; runs existing modules in order, logs and continues past stage failures. |
| 🟢 Production Ready | `onassis/operations.py` — Operations Manager | Pre-flight health gate + Operations Report; aborts and notifies the CEO on a critical dependency failure. |
| 🟡 Partially Ready | `onassis/orchestrator.py` — Legacy content pipeline | Original v0.1 pipeline; still wires the placeholder Publisher/Analytics agents. Superseded by the Daily Cycle for production. |
| 🔴 Development Only | `onassis/agents/publisher.py` — Publisher agent (placeholder) | Intentional no-op from v0.1; superseded by onassis/publishing.py (PublisherService). |
| 🔴 Development Only | `onassis/agents/analytics.py` — Analytics agent (placeholder) | Reports local counts only; superseded by onassis/analytics.py (AnalyticsEngine). |

## Subsystem verification

| Status | Subsystem | Detail |
|---|---|---|
| 🔴 Development Only | Etsy reading | Connector implemented; Etsy credentials missing. |
| 🔴 Development Only | Etsy publishing | Draft publisher implemented; Etsy write credentials missing. |
| 🔴 Development Only | Pinterest publishing | Not implemented — the Pinterest connector has no publishing path and its analytics fetch is a no-op. |
| 🟡 Partially Ready | Analytics collection | Engine ready; Etsy source needs credentials, Pinterest pending. |
| 🟢 Production Ready | Revenue collection | Engine ready; populates from the Etsy sync once credentials are set. |
| 🟢 Production Ready | Compliance | Veto authority active. |
| 🟢 Production Ready | CEO | Risk-adjusted ROI decisions; deterministic. |
| 🟢 Production Ready | Daily Cycle | Single execution entry point; resilient to stage failures. |
| 🟢 Production Ready | Operations Manager | Health gate + reporting; aborts on critical dependency failure. |

## Blockers

| Priority | Module | Description | Estimated effort | Recommended fix |
|---|---|---|---|---|
| **Critical** | Etsy API client (read) | Etsy credentials are missing (ETSY_API_KEY / ETSY_ACCESS_TOKEN / ETSY_SHOP_ID). | 1-2 hours (register an Etsy app + complete OAuth) | Register an Etsy Open API v3 app, complete the OAuth flow, and set ETSY_API_KEY, ETSY_ACCESS_TOKEN and ETSY_SHOP_ID. |
| **Critical** | Etsy Operations Connector (read) | Etsy credentials are missing (ETSY_API_KEY / ETSY_ACCESS_TOKEN / ETSY_SHOP_ID). | 1-2 hours (register an Etsy app + complete OAuth) | Register an Etsy Open API v3 app, complete the OAuth flow, and set ETSY_API_KEY, ETSY_ACCESS_TOKEN and ETSY_SHOP_ID. |
| **Critical** | Analytics Collector | Etsy credentials are missing (ETSY_API_KEY / ETSY_ACCESS_TOKEN / ETSY_SHOP_ID). | 1-2 hours (register an Etsy app + complete OAuth) | Register an Etsy Open API v3 app, complete the OAuth flow, and set ETSY_API_KEY, ETSY_ACCESS_TOKEN and ETSY_SHOP_ID. |
| **Critical** | Autonomous Publisher | Etsy credentials are missing (ETSY_API_KEY / ETSY_ACCESS_TOKEN / ETSY_SHOP_ID). | 1-2 hours (register an Etsy app + complete OAuth) | Register an Etsy Open API v3 app, complete the OAuth flow, and set ETSY_API_KEY, ETSY_ACCESS_TOKEN and ETSY_SHOP_ID. |
| **High** | Pinterest connector | PinterestConnector.fetch_metrics() is not implemented — it returns an empty list even when configured, so no Pinterest metrics are ever collected and Pinterest publishing does not exist. | 1-2 days (map the Pinterest v5 analytics API onto metric rows) | Implement fetch_metrics() against the Pinterest v5 analytics endpoint, mapping impressions/saves/outbound clicks/CTR onto the canonical metric rows. |
| **Medium** | Pinterest connector | Pinterest credentials are missing (PINTEREST_ACCESS_TOKEN / PINTEREST_AD_ACCOUNT_ID). | 1-2 hours (complete Pinterest OAuth) | Create a Pinterest app, complete OAuth, and set PINTEREST_ACCESS_TOKEN and PINTEREST_AD_ACCOUNT_ID. |
| **Medium** | Listing Factory | Mock-up images are 1x1 placeholder PNGs (source='placeholder'); no real product imagery is generated. | 2-3 days (integrate an image/mock-up generator) | Replace _PLACEHOLDER_PNG with a real image-generation or mock-up service that fills each required mock-up slot. |
| **Low** | Autonomous Publisher | Live publishing is not enabled — only Dry Run and Draft modes are active; listings are never made live automatically. | 0.5 day (enable + verify the live path) | When ready for true live listings, add 'live' to publishing.enabled_modes and extend the draft client to set a live state. Drafts are the safe default until then. |
| **Low** | Legacy content pipeline | Uses the placeholder Publisher and Analytics agents, not the production PublisherService/AnalyticsEngine. | 0 days (use the Daily Cycle path in production) | Drive production via the Operations Manager / Daily Cycle; keep the legacy pipeline only for the --once content demo. |
| **Low** | Publisher agent (placeholder) | No-op placeholder agent; does not publish anything. | 0 days (already superseded) | Use onassis/publishing.py for real publishing; this agent remains only as the legacy pipeline slot. |
| **Low** | Analytics agent (placeholder) | Placeholder agent reports local DB counts, not real marketplace metrics. | 0 days (already superseded) | Use onassis/analytics.py for real metric collection; this agent remains only as the legacy pipeline slot. |

## Final checklist

- ✅ AI provider (Anthropic API key)
- ❌ Etsy read credentials
- ❌ Etsy write credentials (publishing)
- ❌ Pinterest OAuth credentials
- ❌ Pinterest analytics/publishing implemented
- ❌ Listing mock-up images (real, not placeholder)
- ❌ Live publishing enabled
- ✅ Database integrity

---

### What this means

ONASSIS is **functionally complete and production-grade in its decision, finance, governance, and orchestration core** — every deterministic module and the AI-backed content/compliance path are Production Ready. The only things standing between the current build and a real end-to-end autonomous cycle are **external credentials and two known dev-only components**:

1. **Etsy credentials** (read + write) — unblocks reading, revenue import, analytics, and Draft publishing. _Highest priority, lowest effort._
2. **Pinterest** — the connector is a no-op (analytics fetch unimplemented, no publishing path). Needs credentials **and** an implementation.
3. **Listing mock-up images** — currently placeholder PNGs; need a real image/mock-up generator before listings are visually complete.

Live publishing is intentionally disabled (Draft mode only) and is a deliberate safety default, not a defect.

---

## Operations runbook (deploy · backup · rollback · health)

_Manually maintained (Sprint 38). Root-level scripts wrap the routine operations
so a normal deployment or health check is one command — no SSH gymnastics, no
remembered flags. Each script is self-contained and overridable via the
environment (`ONASSIS_SERVICE`, `ONASSIS_HEALTH_URL`, `ONASSIS_LOCAL_URL`, …)._

### Normal deployment — one command
```bash
cd ~/Onassis
./deploy.sh
```
`deploy.sh` **backs up first** (and stops if the backup fails), records the
current commit as the rollback point, prints the old → new commit SHAs, pulls
`origin/<current-branch>`, reinstalls dependencies **only if `requirements.txt`
changed**, restarts the service, and verifies `systemctl is-active` **and** the
public health endpoint. Any failure exits non-zero and points you at
`./rollback.sh`.

### Backup — timestamped, git-ignored
```bash
./backup.sh          # -> backups/YYYY-MM-DD-HHMMSS/
```
Backs up `data/onassis.db`, `.env`, `config.yaml`, `exports/`, `logs/` plus a
`MANIFEST.txt` (commit, branch, host, contents). Backups live under `backups/`
which is git-ignored — they are never committed.

### Rollback — undo a bad deploy
```bash
./rollback.sh        # asks to confirm;  ./rollback.sh -y  to skip the prompt
```
Hard-resets to the commit recorded by the last `deploy.sh`
(`.onassis_deploy_state`), reinstalls dependencies, restarts the service, and
re-checks health.

### Health check — pass/fail production status
```bash
./health.sh          # exit 0 = PASS, non-zero = FAIL (safe for cron/monitoring)
```
Reports git commit, systemd service status, public API health, `/exports` local
reachability, disk + memory usage, and recent `journalctl` errors, then prints an
overall **PASS/FAIL**.

### Where logs live
* **Application log:** `logs/onassis.log` (rotating — see `logging.max_bytes` /
  `backup_count` in `config.yaml`; rotated files are `logs/onassis.log.1` …).
* **Service log (systemd/journal):** `journalctl -u onassis` (add `-f` to follow,
  `-p err` for errors only).

### Restart ONASSIS
```bash
sudo systemctl restart onassis      # restart
sudo systemctl status  onassis      # check
sudo systemctl restart onassis && ./health.sh   # restart + verify
```

### Edit `.env` safely
`.env` holds live secrets and is git-ignored — never commit it, and back it up
before editing:
```bash
./backup.sh                                     # snapshot current .env
cp .env "backups/.env.$(date +%F-%H%M%S)"       # quick extra copy (optional)
${EDITOR:-nano} .env                            # edit
sudo systemctl restart onassis                  # apply (config is read at startup)
./health.sh                                     # confirm still healthy
```
Only the six protection controls and integration keys belong in `.env`; all
business logic stays in `config.yaml`. After any `.env` change the service must
be **restarted** to pick it up.
