# ONASSIS v0.1 — runtime dependencies
# Kept intentionally minimal. Everything here is pure-Python and widely
# available so the project runs out of the box without an LLM API key.

PyYAML>=6.0          # Parsing config.yaml
python-dotenv>=1.0   # Loading environment variables from .env
schedule>=1.2        # Lightweight in-process daily scheduler
anthropic>=0.69      # Anthropic API client — powers LLM content generation
fastapi>=0.110       # REST API — the service interface (Swagger at /docs)
uvicorn[standard]>=0.29  # ASGI server to run the FastAPI app
jinja2>=3.1           # Operations Centre templates (the browser control room)
Pillow>=10.0         # Renders the real commercial artwork/mock-ups (Artwork Studio)
